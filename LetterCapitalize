var LetterCapitalize = function(str){

 var words = str.split(" ");
 for(var i=0;i<words.length;i++){
    words[i] = words[i].toUpperCase().substr(-words[i].length,1) + words[i].slice(1,words[i].length);    
 }
 return words.join(" ");
};
